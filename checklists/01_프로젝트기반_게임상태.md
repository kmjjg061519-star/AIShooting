# Checklist 01. 프로젝트 기반 및 게임 상태

## 정적 확인

- [x] `AIShooting.Build.cs`에 `EnhancedInput`이 없다.
- [x] `Core`, `CoreUObject`, `Engine`, `InputCore`, `AIModule`만 필요한 범위로 선언되어 있다.
- [x] `DefaultInput.ini`의 기본 입력 클래스가 `PlayerInput`과 `InputComponent`이다.
- [x] WASD 이동 축, 방향키 발사 액션 4종, Restart 액션이 정의되어 있다.
- [x] `AAIShootingGameModeBase`와 `AAIShootingGameState`가 생성되어 있다.
- [x] `LogAIShooting` 카테고리가 선언 및 정의되어 있다.
- [x] 두 클래스의 Tick이 비활성화되어 있다.

## 빌드 및 Editor 확인

- [x] `AIShootingEditor Win64 Development` 빌드가 성공한다.
- [ ] Unreal Editor에서 프로젝트가 정상적으로 열린다.
- [ ] 빈 프로토타입 맵에서 PIE 시작과 종료가 가능하다.
- [ ] Output Log에 새 Error 또는 반복 Warning이 없다.

## 통과 기준

- [ ] 모든 필수 항목을 통과했다.
- [ ] 실패 또는 미검증 항목이 없다.

## 결과 기록

- 실행 날짜: 2026-08-28
- UE 버전: 5.8.1 (`AIShooting.uproject` EngineAssociation: 5.8)
- 정적 확인 결과: `통과` (근거: Build.cs, DefaultInput.ini, GameMode/GameState, LogAIShooting 선언·정의 및 두 생성자의 Tick 설정을 파일 기준 확인)
- 빌드 결과: `통과` (명령: `D:\\UE_5.8\\Engine\\Binaries\\DotNET\\UnrealBuildTool\\UnrealBuildTool.dll AIShootingEditor Win64 Development "C:\\Users\\user\\Documents\\Unreal Projects\\AIShooting\\AIShooting.uproject" -WaitMutex -NoHotReload -Force`; 로그: `Result: Succeeded`, 총 1.94초)
- Editor 실행 결과: `미검증` (GUI Unreal Editor에서 프로젝트를 열어 확인하지 못함)
- PIE 결과: `미검증` (GUI PIE의 시작·종료 및 Output Log를 확인하지 못함)
- 무인 게임 월드 로드 결과: `통과` (명령줄 `-game -nullrhi -nosound -unattended` 테스트가 Exit 0으로 종료되었고, `Stage1_MapLoadValidation.log`에 `Game class is 'AIShootingGameModeBase'` 및 `Load map complete /Game/Prototype/Maps/L_Prototype`가 기록됨)
- 발견한 문제 또는 미검증 사유: GUI Editor/PIE 세션 및 해당 Output Log의 새 Error·반복 Warning은 검증하지 못했다. 무인 로드 로그의 Engine 플러그인 Warning은 GUI Output Log 항목의 통과 근거로 사용하지 않았다.
- 다음 Task 진행 가능 여부: `불가` (GUI Editor, PIE 및 GUI Output Log 항목이 미검증)

> `통과`는 실제로 확인한 항목에만 사용한다. 빌드, Editor 또는 PIE를 실행할 수 없었던 항목은 반드시 `미검증`과 사유를 기록하며, `미검증` 또는 `실패` 항목이 있으면 다음 Task로 진행하지 않는다.
