# Checklist 03. 플레이어 이동

## 사전 조건

- [ ] Checklist 02를 통과했다.
- [x] `BP_AIShootingPlayer`가 현재 GameMode의 Default Pawn이다.

## 컴포넌트 및 Primitive

- [x] SphereComponent가 Root이고 반지름이 50 uu이다.
- [x] StaticMeshComponent에 `/Engine/BasicShapes/Cone.Cone`이 기본 할당되어 있다.
- [x] Cone 원본 약 100×100×100 uu에 Relative Scale `(0.7, 0.7, 1.0)`이 적용된다.
- [x] Cone이 화면 위쪽 +Z 방향을 향한다.
- [x] VisualComponent의 Collision이 `NoCollision`이다.
- [x] ProjectileSpawnOrigin이 Root에 부착되어 있다.

## 입력 및 이동

- [ ] A 입력 시 월드 -Y로만 이동한다.
- [ ] D 입력 시 월드 +Y로만 이동한다.
- [ ] W 입력 시 월드 +Z로만 이동한다.
- [ ] S 입력 시 월드 -Z로만 이동한다.
- [ ] 단일 축 1초 이동 거리가 약 800 uu이다.
- [ ] 대각선 이동 속도가 단일 축 이동 속도를 초과하지 않는다.
- [ ] 이동 시작 전후의 X 좌표가 같다.
- [ ] 플레이어 중심과 Collision 크기를 고려해 화면 경계 안에 제한된다.

## 회귀 확인

- [ ] Orthographic 카메라와 1024×2048 프레이밍이 유지된다.
- [ ] 방향키를 눌러도 현재 단계에서는 플레이어가 움직이지 않는다.

## 결과 기록

- 실행 날짜: 2026-08-31
- 선행 조건 (Checklist 02): `미통과` (필수 GUI Editor/PIE 카메라 검증이 미완료 상태임)
- 빌드 결과: `통과` (`AIShootingEditor Win64 Development` 빌드 성공)
- 소스/에셋 정적 검증 결과: `통과` (`BP_AIShootingPlayer`와 `BP_AIShootingGameMode`가 생성되어 있고, GameMode의 Default Pawn Class가 전자로 설정됨. Project Default GameMode 및 `L_Prototype` GameMode Override가 `BP_AIShootingGameMode`이며, 맵에는 Player Start 1개와 별도 배치 Pawn 없음. Player Pawn의 Root Sphere 반지름 50 uu, Cone Mesh/Scale/방향, Visual NoCollision, ProjectileSpawnOrigin Root 부착을 확인함)
- 무인 실행 검증 결과: `통과` (무인 Play World에서 `BP_AIShootingGameMode` 및 `BP_AIShootingPlayer`의 정확한 클래스가 생성됨)
- GUI Editor/PIE 검증 결과: `미검증` (Pawn 생성 화면 확인, WASD/방향키 동작, 이동 방향·거리·X 고정·경계, Orthographic 1024×2048 프레이밍을 아직 관찰하지 못함)
- 시작/종료 X 좌표: `미측정` (GUI PIE 측정 필요)
- 이동 거리 측정값: `미측정` (GUI PIE 측정 필요)
- 발견한 문제: Checklist 02의 필수 GUI Editor/PIE 검증이 미완료이므로 단계 선행 조건을 충족하지 못함.
- 다음 Task 진행 가능 여부: `불가` (선행 조건 및 필수 GUI/PIE 항목 미검증)

> `통과`는 해당 항목을 실제로 확인한 검증 종류에만 사용한다. 소스·에셋 확인과
> `-unattended`/`-nullrhi` 무인 실행은 기록할 수 있지만, GUI Editor/PIE에서만 확인할 수 있는
> Pawn 생성, 화면 방향·프레이밍, 입력 이동, 좌표·거리 측정 결과를 대체하지 않는다. 선행 조건 또는
> 필수 GUI/PIE 항목이 `미검증`이나 `실패`이면 다음 Task로 진행하지 않는다.
