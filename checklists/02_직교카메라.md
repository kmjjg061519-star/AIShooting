# Checklist 02. 직교 카메라

## 사전 조건

- [ ] Checklist 01을 통과했다.
- [x] PIE 뷰포트를 1024×2048로 설정했다.

## 클래스 및 컴포넌트

- [x] `AAIShootingCameraActor`가 `ACameraActor`를 상속한다.
- [x] 불필요한 Tick과 추가 컴포넌트가 없다.
- [x] Projection Mode가 Orthographic이다.
- [x] Aspect Ratio가 0.5이고 고정되어 있다.
- [x] Ortho Width가 2048 uu이다.

## 월드 배치

- [x] 카메라 위치가 `(-1500, 0, 0)`이다.
- [x] 회전값이 `(0, 0, 0)`이고 월드 +X를 바라본다.
- [ ] 화면 오른쪽이 월드 +Y와 일치한다.
- [ ] 화면 위쪽이 월드 +Z와 일치한다.
- [ ] 2048×4096 uu 가시 영역의 네 경계 표식이 예상 위치에 보인다.

## 기능 확인

- [x] 가시 영역 Half Extent 계산 결과가 Y=1024, Z=2048 uu이다.
- [x] Clamp 함수가 X를 바꾸지 않고 Y와 Z만 제한한다.
- [ ] Perspective 왜곡이 보이지 않는다.

## 결과 기록

- 실행 날짜: 2026-08-31
- 선행 조건 (Checklist 01): `미검증` (GUI Editor, PIE 및 GUI Output Log 항목이 01에서 미검증으로 남아 있음)
- 정적 검증 결과: `통과` (AAIShootingCameraActor의 상속·Tick·Orthographic·Aspect Ratio 0.5·Ortho Width 2048·Half Extent·YZ Clamp를 소스에서 재확인했고, 맵 자동화 재로드 검증에서 BP_AIShootingCamera 1개의 위치·회전·Player 0 활성화 및 경계 Cube 4개를 확인함)
- 빌드 결과: `통과` (명령: `Build.bat AIShootingEditor Win64 Development AIShooting.uproject -WaitMutex -NoHotReload`; 결과: `Result: Succeeded`, 2026-08-31)
- 무인 실행 결과: `통과` (`ResavePackages -Package=/Game/Prototype/Maps/L_Prototype -unattended`를 2026-08-31에 재실행하여 1/1 패키지 로드·저장, 오류 0건을 확인함; 이전 맵 재로드 검증에서 카메라 1개와 경계 Cube 4개도 확인함. GUI/PIE 통과 근거로 사용하지 않음)
- GUI Editor/PIE 결과: `미검증` (1024×2048 설정 키는 존재하지만 실제 New Editor Window PIE, Orthographic 프레이밍, 화면 +Y/+Z 방향 및 GUI Output Log는 확인하지 못함)
- 카메라 설정 캡처 위치: 미검증 (GUI Details 또는 카메라 파일럿 캡처 없음)
- 경계 Cube 관찰 결과 및 정리 여부: 재로드 검증에서 네 좌표에 존재함을 확인했으나 GUI 화면 관찰 및 정리는 미검증
- 발견한 문제 또는 미검증 사유: Checklist 01의 GUI 선행 조건과 02의 GUI PIE 시각 검증이 남아 있음.
- 다음 Task 진행 가능 여부: `불가` (선행 조건 및 필수 GUI/PIE 항목 미검증)

> `통과`는 실제로 확인한 검증 종류에만 사용한다. 파일·소스 확인은 정적 검증,
> `-game -nullrhi` 등은 무인 실행 검증으로 기록하며, 어느 쪽도 GUI Editor/PIE의
> 화면 프레이밍·방향·Output Log 확인을 대체하지 않는다. 선행 조건 또는 필수 GUI/PIE
> 항목이 `미검증`이나 `실패`이면 다음 Task로 진행하지 않는다.
