# AIShooting 단계별 수행 절차

이 문서는 `Task/README.md`의 01~08 순서를 실제 Unreal Editor 실습 순서로 연결한다. C++ 구현 규칙과 수치는 각 Task 문서를 기준으로 하며, 여기서는 같은 구현 내용을 반복하지 않는다.

## 공통 진행 규칙

1. 한 번에 한 Task만 실행한다.
2. C++ 또는 Config를 변경한 뒤 Unreal Editor를 닫고 `AIShootingEditor`, `Win64`, `Development` 타깃을 빌드한다.
3. 빌드가 성공하면 Editor를 열고 이 문서에 적힌 에셋 생성 및 연결 작업을 수행한다.
4. Play 메뉴의 PIE 실행 방식은 `New Editor Window (PIE)`로 설정하고, Task 02부터 창 크기를 `1024×2048`로 유지한다.
5. 각 단계의 체크리스트를 모두 확인하고 결과 기록란을 작성한다. 실패 또는 미검증 항목이 있으면 다음 단계로 넘어가지 않는다.
6. 전체 작업에서 월드 X는 깊이 고정축, Y는 화면 좌우, Z는 화면 상하로 사용한다. 카메라는 Orthographic을 유지하고 입력은 Legacy Input만 사용한다.

## 1단계. 프로젝트 기반 및 테스트 맵

1. [`Task/01_프로젝트기반_게임상태.md`](Task/01_프로젝트기반_게임상태.md)를 실행한다.
2. `AIShootingEditor Win64 Development` 빌드를 완료하고 Unreal Editor를 연다.
3. Content Browser에서 `/Game/Prototype`, `/Game/Prototype/Blueprints`, `/Game/Prototype/Maps` 폴더를 만든다.
4. 빈 레벨을 만들고 `/Game/Prototype/Maps/L_Prototype`으로 저장한다. 월드에 기본 Pawn이나 카메라가 자동 배치된 템플릿은 사용하지 않는다.
5. `Project Settings > Maps & Modes`에서 Editor Startup Map과 Game Default Map을 `L_Prototype`으로 지정한다.
6. `L_Prototype`의 `World Settings > GameMode Override`를 `AIShootingGameModeBase`로 지정한다.
7. PIE를 시작했다가 종료해 빈 맵과 GameMode/GameState가 오류 없이 로드되는지 확인한다.
8. [`checklists/01_프로젝트기반_게임상태.md`](checklists/01_프로젝트기반_게임상태.md)를 확인하고 결과를 기록한다.

## 2단계. 직교 카메라와 세로형 PIE

1. Checklist 01 통과를 확인한 뒤 [`Task/02_직교카메라.md`](Task/02_직교카메라.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `/Game/Prototype/Blueprints`에 `AAIShootingCameraActor`를 부모로 하는 `BP_AIShootingCamera`를 만든다.
3. `L_Prototype`을 열어 `BP_AIShootingCamera`를 한 개 배치한다. Transform과 CameraComponent 값은 Task 02의 기본값을 유지한다.
4. 배치한 카메라의 `Auto Activate for Player`를 `Player 0`으로 지정한다.
5. 프레이밍 검사용 Cube 네 개를 맵에 임시 배치하고, YZ 가시 영역의 네 모서리를 식별할 수 있게 이름을 정한다. 이 Cube들은 검증 보조물이며 Blueprint로 만들 필요가 없다.
6. Play 메뉴의 고급 설정에서 새 PIE 창의 기준 해상도를 가로 `1024`, 세로 `2048`로 지정한다.
7. 카메라 파일럿 또는 PIE 화면에서 화면 오른쪽이 +Y, 화면 위가 +Z인지 확인한다.
8. [`checklists/02_직교카메라.md`](checklists/02_직교카메라.md)를 확인하고 결과를 기록한다. 경계 검사가 끝난 Cube는 삭제하거나 `/Game/Prototype` 실습용 맵에서만 유지한다.

## 3단계. 플레이어 Blueprint와 게임 규칙 연결

1. Checklist 02 통과를 확인한 뒤 [`Task/03_플레이어이동.md`](Task/03_플레이어이동.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `/Game/Prototype/Blueprints`에 다음 Blueprint를 만든다.
   - `AAIShootingPlayerPawn`을 부모로 하는 `BP_AIShootingPlayer`
   - `AAIShootingGameModeBase`를 부모로 하는 `BP_AIShootingGameMode`
3. `BP_AIShootingPlayer`의 Components와 Class Defaults에서 상속 컴포넌트, Cone 외형, 충돌 크기 및 방향이 Task 03의 기본값과 일치하는지 확인한다. Blueprint Event Graph에는 이동 로직을 중복 작성하지 않는다.
4. `BP_AIShootingGameMode`의 Class Defaults에서 Default Pawn Class를 `BP_AIShootingPlayer`, Player Controller Class를 `AIShootingPlayerController`, Game State Class를 `AIShootingGameState`로 지정한다.
5. `Project Settings > Maps & Modes`의 Default GameMode와 `L_Prototype > World Settings > GameMode Override`를 모두 `BP_AIShootingGameMode`로 변경한다.
6. 맵에 플레이 시작 위치를 나타낼 `Player Start`를 한 개 배치한다. 카메라가 보는 YZ 영역의 중앙을 기준으로 놓고, Pawn의 고정 X가 카메라 촬영 평면과 일치하도록 X 위치를 정한다.
7. PIE에서 생성된 Pawn이 `BP_AIShootingPlayer`인지 확인한다. 플레이어 Pawn을 맵에 별도로 중복 배치하지 않는다.
8. [`checklists/03_플레이어이동.md`](checklists/03_플레이어이동.md)를 확인하고, X 좌표와 축별 이동 거리 측정값을 기록한다.

## 4단계. 총알 Blueprint 연결과 8방향 발사

1. Checklist 03 통과를 확인한 뒤 [`Task/04_방향키총알발사.md`](Task/04_방향키총알발사.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `/Game/Prototype/Blueprints`에 `AAIShootingPlayerProjectile`을 부모로 하는 `BP_AIShootingPlayerProjectile`을 만든다.
3. Blueprint의 상속 컴포넌트에서 Cube 외형, Root 충돌 및 ProjectileMovement 값이 Task 04의 기본값을 유지하는지 확인한다. Event Graph에 별도 발사 또는 이동 Tick을 추가하지 않는다.
4. `BP_AIShootingPlayer`의 Class Defaults에서 Projectile Class를 `BP_AIShootingPlayerProjectile`로 지정하고 컴파일·저장한다.
5. `L_Prototype`에서 PIE를 실행해 WASD 이동과 방향키 발사를 동시에 시험한다. 8방향은 단일 방향 네 가지와 대각선 네 가지를 각각 나누어 확인한다.
6. 화면 밖 총알 제거를 확인할 때도 카메라 Projection Mode와 PIE 해상도를 변경하지 않는다.
7. [`checklists/04_방향키총알발사.md`](checklists/04_방향키총알발사.md)를 확인하고 8방향 결과와 측정 발사 간격을 기록한다.

## 5단계. 적 Blueprint 수동 배치와 추적

1. Checklist 04 통과를 확인한 뒤 [`Task/05_적추적AI.md`](Task/05_적추적AI.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `/Game/Prototype/Blueprints`에 `AAIShootingEnemyPawn`을 부모로 하는 `BP_AIShootingEnemy`를 만든다.
3. Blueprint의 Class Defaults에서 AI Controller Class가 `AIShootingEnemyAIController`이고 Auto Possess AI가 `Placed in World or Spawned`를 포함하는지 확인한다.
4. Sphere 외형과 Root 충돌이 Task 05의 기본값인지 확인하되, Blueprint Event Graph에 NavMesh 이동이나 별도 추적 로직을 만들지 않는다.
5. `BP_AIShootingEnemy` 한 개를 `L_Prototype`의 카메라 가시 영역 상단 안쪽에 수동 배치한다. X는 Player Start에서 생성되는 플레이어의 X와 같게 맞춘다.
6. 맵에는 `NavMeshBoundsVolume`, Behavior Tree, Blackboard를 추가하지 않는다.
7. PIE에서 적의 YZ 추적을 확인한다. 플레이어가 없는 경우의 안정성 검사는 임시로 Player Start 또는 Default Pawn 연결을 해제해 실행한 뒤 즉시 원래 설정으로 복구한다.
8. [`checklists/05_적추적AI.md`](checklists/05_적추적AI.md)를 확인하고 적의 시작·종료 X 좌표와 추적 결과를 기록한다.

## 6단계. 충돌 설정과 전투 생명 주기

1. Checklist 05 통과를 확인한 뒤 [`Task/06_전투충돌_생명.md`](Task/06_전투충돌_생명.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `Project Settings > Engine > Collision`에서 Task 06이 정한 Object Channel 또는 Collision Profile이 존재하는지 확인한다. 이름과 응답 규칙은 C++/Config 구현과 동일하게 유지한다.
3. `BP_AIShootingPlayer`, `BP_AIShootingPlayerProjectile`, `BP_AIShootingEnemy`를 각각 열어 Root Sphere만 게임플레이 Overlap을 만들고 Visual Mesh는 `NoCollision`인지 확인한다.
4. 변경한 충돌 설정이 Blueprint 인스턴스에 반영되지 않았다면 각 Root Sphere의 Collision Preset을 올바른 프로젝트 프로필로 다시 선택하고 Blueprint를 컴파일·저장한다.
5. `L_Prototype`에서 수동 배치한 적의 위치를 조절해 다음 PIE 세션을 각각 분리해 실행한다.
   - 플레이어와 적의 직접 접촉
   - 총알과 적의 접촉
   - 짧은 시간에 여러 Overlap이 발생하는 상황
6. HP가 0이 된 세션은 현재 단계에서 이동과 발사가 멈추는지만 확인하고, 재시작 동작은 아직 연결하지 않는다.
7. [`checklists/06_전투충돌_생명.md`](checklists/06_전투충돌_생명.md)를 확인하고 HP 변화 및 중복 이벤트 여부를 기록한다.

## 7단계. GameMode 적 생성 설정

1. Checklist 06 통과를 확인한 뒤 [`Task/07_적자동생성.md`](Task/07_적자동생성.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `BP_AIShootingGameMode`를 열고 Class Defaults에서 Enemy Class를 `BP_AIShootingEnemy`로 지정한다.
3. Spawn Interval과 Spawn Margin은 먼저 Task 07의 기본값으로 두고, 가시 영역 검증이 쉬운 값이 필요하면 Blueprint 기본값에서만 조정한다.
4. Task 05에서 수동 배치한 `BP_AIShootingEnemy`는 맵에서 삭제해 자동 생성 결과와 구분한다.
5. `L_Prototype > World Settings`가 계속 `BP_AIShootingGameMode`를 사용하며, 맵에 카메라와 Player Start가 각각 하나씩 있는지 확인한다.
6. PIE에서 시간 간격, 생성 위치, 생성 직후 추적을 관찰한다. World Outliner를 사용해 한 번의 Timer 호출마다 적 한 개가 늘어나는지 확인한다.
7. Enemy Class 누락 시 검증은 `BP_AIShootingGameMode`를 복제하지 말고 해당 속성을 잠시 `None`으로 바꾸어 실행한 뒤, 반드시 `BP_AIShootingEnemy`로 복구한다.
8. [`checklists/07_적자동생성.md`](checklists/07_적자동생성.md)를 확인하고 설정 간격, 실제 관찰 결과 및 생성 위치 범위를 기록한다.

## 8단계. 게임 오버·재시작과 전체 루프

1. Checklist 07 통과를 확인한 뒤 [`Task/08_게임오버_재시작.md`](Task/08_게임오버_재시작.md)를 실행한다.
2. Editor 타깃 빌드 성공 후 `BP_AIShootingGameMode`, `BP_AIShootingPlayer`, `BP_AIShootingEnemy`, `BP_AIShootingPlayerProjectile`을 열어 컴파일 오류나 이전 단계의 임시 Override가 없는지 확인한다.
3. `Project Settings > Input`에서 Restart가 R 키를 사용하는 Legacy Action Mapping인지 확인한다. Enhanced Input 에셋이나 Mapping Context는 만들지 않는다.
4. `L_Prototype`을 저장하고 `New Editor Window (PIE)`, `1024×2048`로 실행한다.
5. Playing 상태에서 R을 눌러 레벨이 다시 시작되지 않는지 먼저 확인한다.
6. 적과 반복 접촉해 HP를 0으로 만들고, 게임 오버 표시와 플레이어·적·총알·Timer 정지를 확인한다.
7. GameOver 상태에서 R을 눌러 현재 `L_Prototype`이 다시 로드되는지 확인한다. 재시작 전후 World Outliner와 화면을 비교해 중복 Pawn, 카메라 또는 적이 남지 않는지 확인한다.
8. 새 세션에서 이동, 8방향 발사, 적 자동 생성·추적, 총알 처치, 접촉 피해, 게임 오버, R 재시작을 처음부터 끝까지 한 번 더 수행한다.
9. 전체 세션 동안 플레이 객체의 X가 고정되고, Orthographic 1024×2048 프레이밍이 유지되는지 확인한다.
10. [`checklists/08_게임오버_재시작.md`](checklists/08_게임오버_재시작.md)를 확인하고 최종 결과 기록을 작성한다. 미검증 항목이 있다면 사유를 적고 완료로 처리하지 않는다.

## 최종 완료 조건

- Task 01~08이 번호 순서대로 실행되었다.
- Checklist 01~08의 필수 항목과 결과 기록이 모두 완료되었다.
- `L_Prototype`에서 이동 → 발사 → 적 생성·추적 → 피격·처치 → 게임 오버 → 재시작의 핵심 루프가 동작한다.
- 전체 루프에서 YZ 평면 제약, Orthographic 카메라, 1024×2048 기준 화면, Legacy Input 조건이 유지된다.
